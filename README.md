# Kratos-The-Waiter-Robot
Kratos is a fully automated triple microcontroller robot that follows a line, can detect an obstacle and stop until it's removed. It can sense colours, read voltage, current, and RPM using optical encoders. It displays those values on an OLED. It can also detect images which it is trained for and make decisions accordingly.
